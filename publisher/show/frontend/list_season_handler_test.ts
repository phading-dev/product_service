import { TEST_RUNNER } from "@selfage/test_runner";

TEST_RUNNER.run({
  name: "ListSeasonHandlerTest",
  cases: [{
    name: "Success",
    execute: async () => {

    },
    tearDown: async () => {

    }
  }]
});
